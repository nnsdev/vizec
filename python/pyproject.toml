[project]
name = "vizec-speech-sidecar"
version = "0.1.0"
description = "Speech separation + transcription sidecar for Vizec"
requires-python = ">=3.10"
dependencies = ["demucs>=4.0.1", "faster-whisper>=1.0.3", "numpy>=1.24", "torch>=2.1.0"]

[tool.uv]
package = false
